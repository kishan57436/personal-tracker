# .env

# --- Database Configuration ---
# Your connection string from MongoDB Atlas or your local MongoDB instance.
MONGO_URI="mongodb://localhost:27017/tracker"

# --- Authentication ---
# A long, random, and secret string for signing JSON Web Tokens (JWT).
JWT_SECRET="kishan"

# --- Server Configuration ---
# The port your Express server will run on. 5000 is a common choice for development.
PORT=5000